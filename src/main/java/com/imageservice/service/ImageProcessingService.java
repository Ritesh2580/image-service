package com.imageservice.service;

import org.springframework.stereotype.Service;

@Service
public class ImageProcessingService {
    // Deprecated: use dedicated services (codec + operations + storage)
}
